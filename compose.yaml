services:
  postgres:
    image: 'postgres:latest'
    environment:
        - 'POSTGRES_DB=quizbot'
        - 'POSTGRES_PASSWORD=secret'
        - 'POSTGRES_USER=dalekiymars'
    ports:
        - '5435:5432'